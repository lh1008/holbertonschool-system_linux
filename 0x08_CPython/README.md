# 0x08. CPython

### Big O Notation in Answer File

- `O(1)`
- `O(n)`
- `O(n!)`
- n square -> `O(n^2)`
- log(n) -> `O(log(n))`
- n * log(n) -> `O(nlog(n))`

### Tasks

_**0. Lists big O**_
Write the big O notations for the following lists operations (one per line, in this exact order).

_**1. CPython #0: Python lists**_
Create a C function that prints some basic info about Python lists.

_**2. CPython #1: PyBytesObject**_
Create two C functions that print some basic info about Python lists and Python bytes objects.

_**3. CPython #2: PyFloatObject**_
Create three C functions that print some basic info about Python lists, Python bytes an Python float objects.

_**4. CPython #3: Python Strings**_
Create a function that prints Python strings.

_**5. CPython Integers**_
Create a C function that prints Python integers.
